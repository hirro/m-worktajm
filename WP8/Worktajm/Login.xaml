<ContentDialog
    x:Class="Worktajm.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktajm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Login to Worktajm"
    PrimaryButtonText="sign in"  
    SecondaryButtonText="cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <TextBox Name="email" Header="Email address"/>
        <PasswordBox  Name="password" Header="Password"/>
        <CheckBox Name="savePassword" Content="Save password"/>

        <!-- Content body -->
        <TextBlock Name="body" Style="{StaticResource MessageDialogContentStyle}" TextWrapping="Wrap">
            Enter your username and password to login start reporting time on your lovely Windows Phone.
            <LineBreak/>
            If you are not registered yet, do it using the register button below.
            <LineBreak/>
        </TextBlock>
        <StackPanel VerticalAlignment="Bottom" >
            <Button Content="Register" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>
